# Application
ENVIRONMENT=development
PORT=8081

# Database
# Option 1: Use DATABASE_URL (recommended for Railway/cloud providers)
DATABASE_URL=postgresql://user:password@host:port/dbname

# Option 2: Individual database credentials (for local development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=healthcare_market
DB_SSLMODE=disable

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
